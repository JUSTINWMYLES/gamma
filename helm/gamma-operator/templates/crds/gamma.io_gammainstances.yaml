{{- if .Values.crds.install }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    {{- include "gamma-operator.labels" . | nindent 4 }}
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gammainstances.gamma.io
spec:
  group: gamma.io
  names:
    kind: GammaInstance
    listKind: GammaInstanceList
    plural: gammainstances
    shortNames:
    - gi
    singular: gammainstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.serverReadyReplicas
      name: Server
      type: integer
    - jsonPath: .status.clientReadyReplicas
      name: Client
      type: integer
    - jsonPath: .status.redisReady
      name: Redis
      type: boolean
    - jsonPath: .status.ttsReady
      name: TTS
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GammaInstance is the Schema for the gammainstances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GammaInstanceSpec defines the desired state of a Gamma deployment.
            properties:
              audioOverlay:
                description: AudioOverlay configures infrastructure for the Audio
                  Overlay game.
                properties:
                  objectStore:
                    description: |-
                      ObjectStore enables storage of recorded Audio Overlay clips in the shared
                      SeaweedFS/S3-compatible object store. Browsers still fetch clips through the
                      Gamma server proxy, never directly from the object store.
                    properties:
                      bucketName:
                        default: gamma-audio-overlay-clips
                        description: BucketName is the bucket used for recorded Audio
                          Overlay clips.
                        type: string
                      enabled:
                        default: false
                        description: |-
                          Enabled stores Audio Overlay clips in the shared S3-compatible object store
                          instead of sending base64 payloads during playback whenever the store is
                          available. When false, Audio Overlay keeps the inline/base64 behavior.
                        type: boolean
                      prefix:
                        default: audio-overlay-clips
                        description: Prefix namespaces Audio Overlay clip objects
                          within the bucket.
                        type: string
                    type: object
                type: object
              autoscaling:
                description: Autoscaling configuration for the server deployment.
                properties:
                  enabled:
                    description: Whether to create an HPA for the server deployment.
                    type: boolean
                  maxReplicas:
                    default: 10
                    description: Maximum number of replicas.
                    format: int32
                    type: integer
                  minReplicas:
                    default: 1
                    description: Minimum number of replicas.
                    format: int32
                    minimum: 1
                    type: integer
                  targetCPUUtilizationPercentage:
                    default: 70
                    description: Target CPU utilization percentage.
                    format: int32
                    type: integer
                required:
                - enabled
                type: object
              client:
                description: Client configuration for the nginx SPA frontend.
                properties:
                  image:
                    description: Container image for the Gamma client (nginx SPA).
                    type: string
                  replicas:
                    default: 1
                    description: Number of client pod replicas.
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  resources:
                    description: Resource requirements for client pods.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  serverUrl:
                    description: |-
                      ServerURL overrides the public Colyseus base URL consumed by browsers.
                      This value is injected into the client container at runtime and should
                      point at a browser-reachable endpoint such as wss://gamma.example.com/ws.
                      When omitted, the client derives the URL from window.location.
                    type: string
                required:
                - image
                type: object
              networking:
                description: Networking configuration (ingress).
                properties:
                  ingress:
                    description: Ingress configuration.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Extra ingress annotations.
                        type: object
                      className:
                        description: Ingress class name.
                        type: string
                      enabled:
                        description: Whether to create an Ingress resource.
                        type: boolean
                      host:
                        description: Hostname for the Ingress resource.
                        type: string
                      tls:
                        description: TLS configuration.
                        properties:
                          enabled:
                            description: Whether to enable TLS.
                            type: boolean
                          secretName:
                            description: Secret name containing TLS certificate.
                            type: string
                        type: object
                    type: object
                type: object
              observability:
                description: Observability configures OpenTelemetry tracing and metrics
                  export.
                properties:
                  enabled:
                    description: |-
                      Whether to enable OpenTelemetry telemetry.
                      When false the operator injects OTEL_ENABLED=false, disabling all traces
                      and metrics regardless of the image's built-in defaults.
                      When true the operator injects OTEL_ENABLED=true and, if otlpEndpoint is
                      set, OTEL_EXPORTER_OTLP_ENDPOINT pointing at your OTEL Collector.
                      When the field is omitted entirely, no OTEL_* vars are injected and the
                      server falls back to its own built-in defaults.
                    type: boolean
                  otlpEndpoint:
                    description: |-
                      OTLPEndpoint is the base URL of an OpenTelemetry Collector that accepts
                      OTLP/HTTP (e.g. http://otel-collector.monitoring.svc:4318).
                      Traces are sent to <otlpEndpoint>/v1/traces and metrics to
                      <otlpEndpoint>/v1/metrics every 10 seconds.
                      Required when enabled is true and you want telemetry to reach a collector;
                      omit to keep the server's default (http://localhost:4318).
                    type: string
                  serviceName:
                    description: |-
                      ServiceName overrides the OTEL service name reported in traces and metrics.
                      Defaults to "gamma-server" when not set.
                    type: string
                type: object
              redis:
                description: Redis configuration for game state persistence.
                properties:
                  enabled:
                    default: true
                    description: Whether to deploy a Redis StatefulSet for game state.
                    type: boolean
                  image:
                    default: redis:7-alpine
                    description: Container image for Redis.
                    type: string
                  resources:
                    description: Resource requirements for Redis pods.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  storage:
                    description: Persistent storage configuration.
                    properties:
                      size:
                        default: 1Gi
                        description: PVC size for Redis data.
                        type: string
                      storageClassName:
                        description: StorageClass name. Empty uses cluster default.
                        type: string
                    type: object
                type: object
              region:
                description: Region is an informational label for future multi-region
                  federation.
                type: string
              server:
                description: Server configuration for the Colyseus game server.
                properties:
                  env:
                    description: Additional environment variables injected into server
                      pods.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  image:
                    description: Container image for the Gamma Colyseus server.
                    type: string
                  port:
                    default: 2567
                    description: Port the Colyseus server listens on.
                    format: int32
                    maximum: 65535
                    minimum: 1024
                    type: integer
                  replicas:
                    default: 1
                    description: |-
                      Number of server pod replicas.
                      IMPORTANT: Colyseus rooms live in local memory. With >1 replica,
                      WebSocket connections may land on a pod that does not host the room,
                      causing "seat reservation expired" errors. Use 1 replica unless you
                      deploy a @colyseus/proxy layer for room-aware routing.
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  resources:
                    description: Resource requirements for server pods.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  secretEnvVars:
                    description: |-
                      SecretEnvVars loads environment variables from Kubernetes Secrets.
                      Use this for sensitive values such as third-party API keys (e.g. a GIF
                      service API key) that must not be stored in plain text in the CR spec.
                    items:
                      description: |-
                        SecretEnvVar describes a single environment variable whose value is sourced
                        from a key in a Kubernetes Secret.
                      properties:
                        name:
                          description: Name of the environment variable to expose
                            in the server container.
                          type: string
                        secretKey:
                          description: SecretKey is the key within the Secret whose
                            value will be used.
                          type: string
                        secretName:
                          description: SecretName is the name of the Kubernetes Secret
                            in the same namespace.
                          type: string
                      required:
                      - name
                      - secretKey
                      - secretName
                      type: object
                    type: array
                required:
                - image
                type: object
              tts:
                description: TTS provisions News Broadcast speech synthesis infrastructure.
                properties:
                  api:
                    description: API configures the HTTP service that accepts jobs
                      and exposes artifacts.
                    properties:
                      image:
                        default: ghcr.io/gamma/gamma-tts-api:latest
                        description: Container image for the Go TTS API.
                        type: string
                      port:
                        default: 8090
                        description: Port the TTS API listens on.
                        format: int32
                        maximum: 65535
                        minimum: 1024
                        type: integer
                      replicas:
                        default: 1
                        description: Number of TTS API pod replicas.
                        format: int32
                        maximum: 100
                        minimum: 1
                        type: integer
                      resources:
                        description: Resource requirements for TTS API pods.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                  config:
                    description: Config contains shared runtime settings applied to
                      the TTS API and worker.
                    properties:
                      bucketName:
                        default: gamma-tts-artifacts
                        description: BucketName is the MinIO bucket used to store
                          synthesized artifacts.
                        type: string
                      modelVersion:
                        default: OpenMOSS-Team/MOSS-TTS-Nano-100M-ONNX
                        description: ModelVersion is recorded on jobs and shared between
                          the API and worker.
                        type: string
                      redisKeyPrefix:
                        default: gamma:tts
                        description: RedisKeyPrefix namespaces TTS queue state inside
                          Redis.
                        type: string
                      requireCustomVoicePack:
                        default: false
                        description: RequireCustomVoicePack keeps the API unready
                          until packaged custom voice packs exist.
                        type: boolean
                      requireWorkerReady:
                        default: true
                        description: RequireWorkerReady keeps the API unready until
                          a healthy worker heartbeat exists.
                        type: boolean
                    type: object
                  enabled:
                    default: false
                    description: Whether to deploy TTS infrastructure and wire the
                      Gamma server to it.
                    type: boolean
                  objectStore:
                    description: ObjectStore configures the self-contained S3-compatible
                      object store (SeaweedFS) used for TTS artifacts.
                    properties:
                      credentials:
                        description: Bootstrap credentials used by the object store
                          and by the TTS API/worker.
                        properties:
                          secretRef:
                            description: |-
                              SecretRef references a Kubernetes Secret that holds object store credentials.
                              The secret must contain keys named "accessKey" and "secretKey".
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      image:
                        default: chrislusf/seaweedfs:4.21
                        description: Container image for the object store.
                        type: string
                      ports:
                        description: Service ports exposed by the object store.
                        properties:
                          api:
                            default: 8333
                            description: API is the S3-compatible API port.
                            format: int32
                            maximum: 65535
                            minimum: 1024
                            type: integer
                          console:
                            default: 9333
                            description: Console is the web console port.
                            format: int32
                            maximum: 65535
                            minimum: 1024
                            type: integer
                        type: object
                      resources:
                        description: Resource requirements for the object store pod.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      storage:
                        description: Persistent storage configuration for TTS artifacts.
                        properties:
                          size:
                            default: 5Gi
                            description: PVC size for object store data.
                            type: string
                          storageClassName:
                            description: StorageClass name. Empty uses cluster default.
                            type: string
                        type: object
                    type: object
                  worker:
                    description: Worker configures the synthesis worker deployment.
                    properties:
                      image:
                        default: ghcr.io/gamma/gamma-tts-worker:latest
                        description: Container image for the TTS worker.
                        type: string
                      replicas:
                        default: 1
                        description: |-
                          Number of worker pod replicas.
                          The worker runtime still processes one job at a time per worker pod.
                        format: int32
                        maximum: 100
                        minimum: 1
                        type: integer
                      resources:
                        description: Resource requirements for worker pods.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                type: object
            required:
            - client
            - server
            type: object
          status:
            description: GammaInstanceStatus defines the observed state of a Gamma
              deployment.
            properties:
              clientEndpoint:
                description: Client service endpoint.
                type: string
              clientReadyReplicas:
                description: Number of ready client replicas.
                format: int32
                type: integer
              conditions:
                description: Status conditions for each component.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: Last observed generation of the spec.
                format: int64
                type: integer
              phase:
                description: Current phase of the deployment.
                enum:
                - Pending
                - Deploying
                - Running
                - Degraded
                - Failed
                type: string
              redisEndpoint:
                description: Redis service endpoint.
                type: string
              redisReady:
                description: Whether Redis is ready.
                type: boolean
              serverEndpoint:
                description: Server service endpoint.
                type: string
              serverReadyReplicas:
                description: Number of ready server replicas.
                format: int32
                type: integer
              ttsApiEndpoint:
                description: TTS API service endpoint.
                type: string
              ttsApiReadyReplicas:
                description: Number of ready TTS API replicas.
                format: int32
                type: integer
              ttsObjectStoreConsoleEndpoint:
                description: TTS object store console endpoint.
                type: string
              ttsObjectStoreEndpoint:
                description: TTS object store API endpoint.
                type: string
              ttsObjectStoreReady:
                description: Whether the TTS object store is ready.
                type: boolean
              ttsReady:
                description: Whether the TTS stack is fully ready.
                type: boolean
              ttsWorkerReadyReplicas:
                description: Number of ready TTS worker replicas.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
