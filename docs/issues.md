Yes proceed with these tasks. Additionally, evaluate, fix and implement all of the following

## Shading 

- Also note that there is still some darker purple looking bands on my computer and phone view, for example the cards and shelf view is in its own little band of color it seems. Also in the instructions screen it seems to be doing that
- The tol of the cards view is a bit cutoff, please add some gap space between the cards, and the books on the top of the shelf view slightly go out of screen when I tap on them when they move up


## Maze game

- The problem with the phones is that when I have to tap on the arrow direction, if I tap too fast it zooms in on the screen, maybe make it so that the user has a joycon or tilt control.
- The map looks ok, maybe just make it so there are 3 paths each round that are somewhat similar for path length, then the user has to decide on a given set of paths

## Tap speed

- The instructions are for the wrong game
- There should be at least a few seconds to show the actual full bracket, then pan in to narrow down to the 2 that are competing in the bracket next. Then give a 3-2-1 count before they start
- Currently there is never a screen that shows that allows the users to start tapping

## Sound replication

- Wrong instructions
- The sound does not actually play before, it is just silent. Do I need to change the audio file type to mp3 or can you work with flac?